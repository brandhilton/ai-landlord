--- a/code.html
+++ b/code.html
@@ -19,31 +19,46 @@
             align-items: center;
         }
 
-        /* Main Game Screen Container */
+        /* Main game screen container - stacks sections vertically */
         #game-screen {
             display: flex;
             flex-direction: column;
-            align-items: center; /* Centers H1 and main-game-layout */
-            gap: 20px; /* Space between H1 and main-game-layout */
+            align-items: center; /* Centers content horizontally */
+            gap: 20px; /* Space between top section and message log */
             width: 100%; /* Take full width available */
         }
 
-        /* NEW: Container for the two main columns (Board/Log & Controls/Debug) */
-        #main-game-layout {
-            display: flex;
-            gap: 20px; /* Space between the left and right columns */
+        /* Wrapper for the top section (main game area + debug controls) */
+        #top-section-wrapper {
+            display: flex;
+            flex-wrap: wrap; /* Allows debug controls to wrap below on smaller screens */
             justify-content: center; /* Center the entire layout horizontally */
             align-items: flex-start; /* Align columns to the top */
-            max-width: 1200px; /* Max width to contain both columns and gap */
+            gap: 20px; /* Space between main game area and debug controls */
+            max-width: 1800px; /* Max width for the entire top section */
             width: 100%; /* Ensure it uses available width up to max */
         }
 
-        /* NEW: Left Column (Board + Message Log) */
-        #left-column {
-            display: flex;
-            flex-direction: column;
-            gap: 20px; /* Space between board and message log */
+        /* Main game area (board + right-side panel) */
+        #main-game-area {
+            display: flex;
+            flex-wrap: wrap; /* Allows right-side panel to wrap below board on very small screens */
+            background-color: white;
+            border: 1px solid #ccc;
+            box-shadow: 0 0 10px rgba(0,0,0,0.1);
+            padding: 20px;
+            border-radius: 8px;
+            flex: 1; /* Allows it to grow/shrink within top-section-wrapper */
+            min-width: 1150px; /* Minimum width for board + right panel */
+            max-width: 1200px; /* Max width for board + right panel */
+            gap: 20px; /* Space between game board and right-side panel */
+        }
+
+        #game-board {
+            display: grid;
+            grid-template-columns: repeat(11, 1fr);
+            grid-template-rows: repeat(11, 1fr);
+            width: 800px; /* Fixed width for the board */
+            height: 800px; /* Fixed height for the board */
+            border: 2px solid black;
+            box-sizing: border-box;
+            background-color: #e0e0e0;
+            position: relative;
             flex-shrink: 0; /* Prevent it from shrinking */
         }
 
-        /* NEW: Right Column (Controls + Player List + Debug Controls) */
-        #right-column {
-            display: flex;
-            flex-direction: column;
-            gap: 20px; /* Space between game controls and debug controls */
-            flex-shrink: 0; /* Prevent it from shrinking */
-            width: 350px; /* Fixed width for the right column elements */
-            min-width: 300px; /* Ensure elements don't get too small */
-            max-width: 400px; /* Max width for consistency */
-        }
-
-        /* Game Board Styling (now a distinct block) */
-        #game-board {
-            width: 800px; /* Fixed width for the entire board container */
-            height: 800px; /* Fixed height for the entire board container */
-            background-color: white;
-            border: 1px solid #ccc;
-            box-shadow: 0 0 10px rgba(0,0,0,0.1);
-            padding: 20px;
-            border-radius: 8px;
-            box-sizing: border-box; /* Include padding and border in the width/height */
-            display: grid; /* Use grid for the board layout itself */
-            grid-template-columns: repeat(11, 1fr);
-            grid-template-rows: repeat(11, 1fr);
-            position: relative;
-            background-color: #e0e0e0; /* Background for board grid area */
-        }
-        
         .board-space {
             border: 1px solid #aaa;
             background-color: #f9f9f9;
@@ -232,11 +247,14 @@
         /* Right-side panel for controls, player list */
         #right-side-panel {
             display: flex;
             flex-direction: column; /* Stack children vertically */
             gap: 20px; /* Space between game controls and player list */
-            background-color: white;
-            border: 1px solid #ccc;
-            box-shadow: 0 0 10px rgba(0,0,0,0.1);
-            padding: 20px;
-            border-radius: 8px;
-            box-sizing: border-box; /* Include padding/border in its fixed width */
+            flex: 1; /* Allows it to grow/shrink within main-game-area */
+            min-width: 300px; /* Minimum width for this panel */
+            max-width: 350px; /* Max width for this panel */
         }
 
         #game-controls {
@@ -301,24 +319,19 @@
             margin-bottom: 3px;
         }
         
-        /* Message Log Area (bottom-left grid cell) */
-        #message-log-area {
-            display: flex;
-            flex-direction: column;
-            gap: 10px; /* Space between message log and toggle button */
-            align-items: center; /* Center log and button horizontally */
-            background-color: white;
-            border: 1px solid #ccc;
-            box-shadow: 0 0 10px rgba(0,0,0,0.1);
-            padding: 20px;
-            border-radius: 8px;
-            box-sizing: border-box; /* Include padding/border in width */
-            width: 800px; /* Match the width of the game board block */
-            min-height: 200px; /* Give it some minimum height */
-        }
-
         #message-log {
             height: 150px;
             border: 1px solid #ccc;
             padding: 10px;
             overflow-y: scroll;
             background-color: #e9e9e9;
             font-size: 0.9em;
             border-radius: 5px;
-            width: 100%; /* Take full width of its parent container */
+            /* NEW: Flex properties for message log */
+            flex: 1; /* Allows it to grow/shrink */
+            min-width: 300px; /* Minimum width */
+            max-width: 1200px; /* Max width to align with main-game-area */
+            box-sizing: border-box; /* Include padding/border in width */
+            margin: 0 auto; /* Center it if it doesn't fill max-width */
         }
         #message-log div {
             margin-bottom: 5px;
@@ -342,10 +355,10 @@
             padding: 20px;
             border: 1px solid #ddd;
             background-color: #fff;
             border-radius: 8px;
-            box-sizing: border-box; /* Include padding/border in its fixed width */
-            display: flex;
-            flex-direction: column;
-            align-items: center;
+            flex: 0 0 350px; /* Fixed width for the debug panel */
+            max-width: 400px;
+            min-width: 300px;
+            box-sizing: border-box;
+            display: flex;
+            flex-direction: column;
+            align-items: center;
         }
         #debug-controls h2 {
             margin-top: 0;
             color: #333;
         }
-        #debug-controls > div { /* Inner debug control groups wrapper */
+        #debug-controls > div { /* Inner debug control groups */
             display: flex;
-            flex-wrap: wrap; /* Allows debug groups to wrap */
+            flex-wrap: wrap;
             gap: 20px;
             justify-content: center;
             width: 100%;
@@ -355,8 +368,7 @@
             padding: 15px;
             border-radius: 5px;
             background-color: #f9f9f9;
-            flex: 1 1 calc(50% - 10px); /* Two columns within debug controls on wider screens */
-            max-width: calc(100% - 20px); /* Ensure it doesn't break out on small screens */
+            flex: 1; /* Allow groups to grow/shrink */
+            min-width: 250px;
+            max-width: 350px;
             box-sizing: border-box;
             display: flex;
             flex-direction: column;
@@ -453,17 +465,19 @@
     </div>
 
     <div id="game-screen" style="display: none;">
-        <h1>Monopoly</h1>
+        <h1>Landlord</h1>
         
-        <div id="main-game-layout">
-            <!-- Left Column: Game Board and Message Log -->
-            <div id="left-column">
-                <div id="game-board"></div> <!-- Game board content will be generated here by JS -->
-                <div id="message-log-area">
-                    <div id="message-log"></div>
-                    <button id="toggle-debug-btn">Toggle Debug Controls</button>
-                </div>
-            </div>
-
-            <!-- Right Column: Game Controls, Player List, and Debug Controls -->
-            <div id="right-column">
+        <!-- Wrapper for the top section (main game area + debug controls) -->
+        <div id="top-section-wrapper">
+            <!-- Main game area (board + right-side panel) -->
+            <div id="main-game-area">
+                <div id="game-board"></div>
+
+                <!-- Right-side panel for controls, player list -->
                 <div id="right-side-panel">
                     <div id="game-controls">
                         <div id="current-player-display"></div>
@@ -474,26 +488,25 @@
 
                         <div id="property-actions" style="display: none;">
                             <h3>Property Actions</h3>
-                            <button id="buy-property-btn" style="display: none;">Buy Property</button>
-                            <button id="build-house-btn" style="display: none;">Build Dwelling</button>
+                            <button id="buy-property-btn" style="display: none;">Buy Holding</button>
+                            <button id="build-house-btn" style="display: none;">Build Dwelling</button>
                             <select id="mortgage-select" class="action-select" style="display: none;"></select>
-                            <button id="mortgage-property-btn" style="display: none;">Mortgage Holding</button>
+                            <button id="mortgage-property-btn" style="display: none;">Mortgage Holding</button>
                             <select id="unmortgage-select" class="action-select" style="display: none;"></select>
-                            <button id="unmortgage-property-btn" style="display: none;">Unmortgage Holding</button>
+                            <button id="unmortgage-property-btn" style="display: none;">Unmortgage Holding</button>
                             <select id="sell-house-select" class="action-select" style="display: none;"></select>
-                            <button id="sell-house-btn" style="display: none;">Sell Dwelling</button>
+                            <button id="sell-house-btn" style="display: none;">Sell Dwelling</button>
                         </div>
 
                         <div id="jail-actions" style="display: none;">
-                            <h3>Detention Block Actions</h3>
+                            <h3>Detention Block Actions</h3>
                             <button id="pay-bail-btn">Pay $50 Bail</button>
-                            <button id="use-jail-card-btn">Use Get Out of Detention Free Card</button>
+                            <button id="use-jail-card-btn">Use Get Out of Detention Free Card</button>
                         </div>
                     </div>
                     <div id="player-list-container"></div>
                 </div>
-
+            </div>
                 <!-- Debug Controls Section -->
                 <div id="debug-controls" style="display: none;">
                     <h2>Debug Controls</h2>
@@ -520,26 +533,29 @@
 
                         <!-- Change Ownership -->
                         <div class="debug-group">
-                            <h4>Change Holding Ownership</h4>
+                            <h4>Change Holding Ownership</h4>
                             <select id="debug-property-select" class="action-select"></select>
                             <select id="debug-owner-select" class="action-select"></select>
                             <button id="debug-change-owner-btn" class="button-group button">Change Owner</button>
                         </div>
 
                         <!-- Manage Houses/Hotels -->
                         <div class="debug-group">
-                            <h4>Manage Dwellings/Fortresses</h4>
+                            <h4>Manage Dwellings/Fortresses</h4>
                             <select id="debug-house-property-select" class="action-select"></select>
-                            <label for="debug-house-count">Dwellings (0-5):</label>
+                            <label for="debug-house-count">Dwellings (0-5):</label>
                             <input type="number" id="debug-house-count" min="0" max="5" value="0">
-                            <button id="debug-set-houses-btn" class="button-group button">Set Dwellings</button>
+                            <button id="debug-set-houses-btn" class="button-group button">Set Dwellings</button>
                         </div>
 
                         <!-- Put Player in Jail -->
                         <div class="debug-group">
-                            <h4>Send to Detention Block</h4>
+                            <h4>Send to Detention Block</h4>
                             <select id="debug-jail-player-select" class="action-select"></select>
-                            <button id="debug-send-to-jail-btn" class="button-group button">Send to Detention</button>
+                            <button id="debug-send-to-jail-btn" class="button-group button">Send to Detention</button>
                         </div>
                     </div>
                 </div>
-            </div>
         </div>
+        <!-- Message Log (now a direct child of game-screen, below top-section-wrapper) -->
+        <div id="message-log"></div>
     </div>
 
     <!-- Property Info Modal -->
@@ -551,8 +567,8 @@
             <p><strong>Price:</strong> $<span id="modal-property-price"></span></p>
             <p><strong>Owner:</strong> <span id="modal-property-owner"></span></p>
             <p><strong>Mortgaged:</strong> <span id="modal-property-mortgaged"></span></p>
-            <p id="modal-property-houses-row"><strong>Dwellings:</strong> <span id="modal-property-houses"></span></p>
-            <p id="modal-property-house-cost-row"><strong>Dwelling Cost:</strong> $<span id="modal-property-house-cost"></span></p>
+            <p id="modal-property-houses-row"><strong>Dwellings:</strong> <span id="modal-property-houses"></span></p>
+            <p id="modal-property-house-cost-row"><strong>Dwelling Cost:</strong> $<span id="modal-property-house-cost"></span></p>
             <p><strong>Mortgage Value:</strong> $<span id="modal-property-mortgage-value"></span></p>
 
             <h4>Rent:</h4>
@@ -561,6 +577,8 @@
             </ul>
         </div>
     </div>
+
+    <button id="toggle-debug-btn" style="margin-top: 10px; padding: 8px 15px; background-color: #6c757d; color: white; border: none; border-radius: 5px; cursor: pointer;">Toggle Debug Controls</button>
 
     <script>
         // --- Game Data ---